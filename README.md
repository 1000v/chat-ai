# 🤖 AI Chat Ecosystem

Современный веб-клиент для общения с различными AI-моделями. Поддержка множества провайдеров, система памяти, ветвление диалогов и многое другое.

## ✨ Возможности

### 💬 Чат
- **Стриминг ответов** — ответы появляются в реальном времени с рендерингом Markdown
- **Варианты ответов** — перегенерация создаёт альтернативные варианты (как в DeepSeek)
- **Редактирование сообщений** — редактирование пользовательских сообщений создаёт новую ветку (как в ChatGPT)
- **Цитирование** — выделите текст и процитируйте его в поле ввода
- **Markdown** — полная поддержка с подсветкой кода

### 🔌 Провайдеры
- **Мульти-провайдер** — подключайте несколько AI-провайдеров одновременно
- **Cody** — поддержка Cody с автоопределением эндпоинтов
- **Ollama** — локальные модели без API ключей
- **Ротация ключей** — до 5 API ключей на провайдер с автопереключением

### 🧠 Память и анализ
- **Профиль пользователя** — система запоминает факты, предпочтения, цели
- **Анализатор** — автоматический анализ диалогов для извлечения информации
- **3 режима работы:**
  - Simple — только чат
  - Standard — анализ с подтверждением
  - Pro — автоматический анализ

### 🎨 Кастомизация
- **Темы** — светлая, тёмная, авто
- **Шрифты** — системный, Inter, Roboto, JetBrains Mono, Fira Code
- **Размер шрифта** — от 12px до 20px
- **Шаблоны промптов** — создавайте и используйте свои системные промпты

### 📱 Интерфейс
- **Адаптивный дизайн** — работает на десктопе и мобильных
- **Сворачиваемый сайдбар** — как в ChatGPT/Claude
- **Анимации** — плавные переходы с Framer Motion

## 🚀 Быстрый старт

```bash
# Клонирование
git clone <repo-url>
cd ai-chat-ecosystem

# Установка зависимостей
npm install

# Запуск
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## ⚙️ Настройка провайдеров

### OpenAI-совместимые API

1. Откройте **Провайдеры** в настройках
2. Нажмите **Добавить провайдер**
3. Укажите:
   - Название
   - Base URL (например `https://api.openai.com/v1`)
   - API ключ
   - Список моделей

### Ollama (локальные модели)

1. Установите [Ollama](https://ollama.ai)
2. Запустите нужные модели: `ollama run llama3`
3. В провайдерах укажите:
   - Base URL: `http://localhost:11434`
   - Включите флаг "Ollama"
   - API ключ не нужен

## 🛠 Технологии

- **Next.js 14** — React фреймворк
- **TypeScript** — типизация
- **Tailwind CSS** — стили
- **Zustand** — стейт-менеджмент
- **Framer Motion** — анимации
- **React Markdown** — рендеринг Markdown

## 📁 Структура

```
src/
├── app/
│   ├── api/proxy/     # Прокси для API запросов
│   ├── globals.css    # Глобальные стили
│   ├── layout.tsx     # Корневой layout
│   └── page.tsx       # Главная страница
├── components/
│   ├── ui/            # UI компоненты (Button, Modal, etc.)
│   ├── ChatView.tsx   # Основной чат
│   ├── Sidebar.tsx    # Боковая панель
│   ├── Settings.tsx   # Настройки
│   └── ...
├── store/
│   └── index.ts       # Zustand store
├── types/
│   └── index.ts       # TypeScript типы
└── lib/
    └── utils.ts       # Утилиты
```

## 📝 Лицензия

MIT
